{
  "name": "beereport-data-collector",
  "version": "0.1.0",
  "description": "A service that will listen to MQTT topic and store data into a DB (shared by Beereport App).",
  "main": "index.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Anton Hedstrom",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^10.0.0",
    "knex": "^0.95.6",
    "mqtt": "^4.2.6",
    "pg": "^8.6.0"
  }
}
